# ============================================================
# CONFIGURATION — Safe to commit (no secrets here)
# ============================================================

# Backend API base URL
# The browser always talks to localhost because Docker maps
# backend container port 8000 → host port 8000
VITE_API_URL=http://localhost:8000
